global:
  scrape_interval: 5s
  external_labels:
    service: streamrank

scrape_configs:
  - job_name: streamrank-api
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8200"]
        labels: { component: serving }
